---
"headertweaker": patch
---

- Add a scope indicator to the header overview.
- Fix an issue where the header settings panel was not scrollable, making the Save button inaccessible when adding multiple headers.
- Allow headers to be created by pressing Enter.
- Prevent duplicate URLs from being stored when adding a scope.
- Automatically enable label functionality when adding a label to a header if it is not already enabled.
