---
"headertweaker": patch
---

- Add a scope indicator to the header overview.
- Fix an issue where the header settings panel was not scrollable, causing the Save button to be inaccessible when adding multiple headers.
- Allow headers to be created by pressing Enter.
- Prevent storing duplicate url's when adding a scope
